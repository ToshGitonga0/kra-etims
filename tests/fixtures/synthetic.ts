/**
 * Synthetic test fixtures. Every value here is either taken directly from
 * KRA's own published JSON samples in OSCU_Specification_Document_v2.0.pdf
 * (which already use placeholder-style values like "A123456789Z") or is
 * clearly fake data invented for this SDK's tests. None of these are real
 * taxpayer credentials — do not reuse them against a real KRA sandbox.
 */

export const FIXTURE_PIN = "A123456789Z";
export const FIXTURE_BRANCH_ID = "00";
export const FIXTURE_CMC_KEY = "f0b9831bd2334874b7ec815e40347bc4";
export const FIXTURE_DEVICE_SERIAL = "dev-serial-0001";
